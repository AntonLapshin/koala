import { px } from '@zos/utils';

export const { width, height } = hmSetting.getDeviceInfo();

export const TEXT_SIZE_SMALL = px(12);
export const TEXT_SIZE_MEDIUM = px(16);
export const TEXT_SIZE_LARGE = px(20);
